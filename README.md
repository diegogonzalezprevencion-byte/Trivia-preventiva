# Trivia de la Movilidad

MVP estático de una aplicación corporativa de aprendizaje preventivo gamificado.

## Publicación en GitHub

1. Crea un repositorio nuevo.
2. Sube en la raíz: `index.html`, `styles.css`, `app.js`, `vercel.json`, `.gitignore` y este `README.md`.
3. Confirma el commit en la rama `main`.

## Despliegue en Vercel

1. Selecciona **Add New > Project**.
2. Importa el repositorio de GitHub.
3. Usa **Framework Preset: Other**.
4. Deja vacío **Build Command**.
5. Usa la raíz del repositorio como **Root Directory**.
6. Despliega.

## Credenciales de demostración

- RUT: `12.345.678-5`
- Clave: `Admin123!`

## Advertencia

Esta versión usa `localStorage`. Cada navegador conserva sus propios usuarios, respuestas y sorteos. El despliegue sirve para demostración, pero no para una operación corporativa compartida. Para producción debe agregarse autenticación y base de datos centralizada, además de retirar las credenciales de demostración.
